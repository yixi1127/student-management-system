# 学生管理系统 - 阿里云部署指南

## 前提条件

1. 已购买阿里云ECS服务器（推荐配置：2核4G，CentOS 7.x或Ubuntu 20.04）
2. 已有服务器公网IP
3. 已备案域名（可选，也可以直接用IP访问）

---

## 第一步：连接服务器

### Windows用户
使用PuTTY或Xshell连接：
- 主机：你的服务器公网IP
- 端口：22
- 用户名：root
- 密码：购买服务器时设置的密码

### 或使用阿里云控制台
登录阿里云 → ECS → 实例 → 远程连接

---

## 第二步：安装环境

### 1. 更新系统
```bash
yum update -y  # CentOS
# 或
apt update && apt upgrade -y  # Ubuntu
```

### 2. 安装Node.js
```bash
# 安装nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc

# 安装Node.js 20
nvm install 20
nvm use 20
node -v  # 验证安装
```

### 3. 安装MySQL
```bash
# CentOS
wget https://dev.mysql.com/get/mysql80-community-release-el7-3.noarch.rpm
rpm -ivh mysql80-community-release-el7-3.noarch.rpm
yum install mysql-server -y
systemctl start mysqld
systemctl enable mysqld

# Ubuntu
apt install mysql-server -y
systemctl start mysql
systemctl enable mysql
```

### 4. 配置MySQL
```bash
# 获取临时密码（CentOS）
grep 'temporary password' /var/log/mysqld.log

# 登录MySQL
mysql -u root -p

# 修改密码并创建数据库
ALTER USER 'root'@'localhost' IDENTIFIED BY '你的新密码';
CREATE DATABASE student_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

### 5. 安装Nginx
```bash
# CentOS
yum install nginx -y

# Ubuntu
apt install nginx -y

# 启动Nginx
systemctl start nginx
systemctl enable nginx
```

### 6. 安装PM2（进程管理器）
```bash
npm install -g pm2
```

---

## 第三步：上传项目文件

### 方法1：使用Git（推荐）
```bash
# 在服务器上
cd /var/www
git clone 你的项目仓库地址
cd student-management-system
```

### 方法2：使用FTP工具
使用FileZilla或WinSCP上传项目文件到 `/var/www/student-management-system`

### 方法3：使用SCP命令
在本地电脑执行：
```bash
scp -r E:\develop\项目\administration\student-management-system root@你的服务器IP:/var/www/
```

---

## 第四步：配置后端

### 1. 进入后端目录
```bash
cd /var/www/student-management-system/server
```

### 2. 安装依赖
```bash
npm install
```

### 3. 修改环境配置
```bash
vi .env
```

修改为：
```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=student_management
JWT_SECRET=your-secret-key-change-this-in-production
PORT=3001
```

按 `i` 进入编辑模式，修改完后按 `ESC`，输入 `:wq` 保存退出

### 4. 导入数据库
```bash
mysql -u root -p student_management < /var/www/student-management-system/database-mysql.sql
```

### 5. 启动后端服务
```bash
pm2 start index.js --name student-api
pm2 save
pm2 startup
```

### 6. 验证后端
```bash
curl http://localhost:3001/api
```

---

## 第五步：配置前端

### 1. 修改API地址
```bash
cd /var/www/student-management-system
vi src/utils/request.js
```

修改baseURL为：
```javascript
const request = axios.create({
  baseURL: 'http://你的服务器IP:3001/api',  // 改成服务器IP
  timeout: 10000
})
```

### 2. 构建前端
```bash
npm install
npm run build
```

构建完成后会生成 `dist` 文件夹

---

## 第六步：配置Nginx

### 1. 创建Nginx配置
```bash
vi /etc/nginx/conf.d/student-management.conf
```

添加以下内容：
```nginx
server {
    listen 80;
    server_name 你的服务器IP;  # 或域名

    # 前端静态文件
    location / {
        root /var/www/student-management-system/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 2. 测试并重启Nginx
```bash
nginx -t  # 测试配置
systemctl restart nginx
```

---

## 第七步：配置防火墙

### 开放端口
```bash
# CentOS
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=3001/tcp
firewall-cmd --reload

# Ubuntu
ufw allow 80/tcp
ufw allow 3001/tcp
ufw reload
```

### 阿里云安全组
1. 登录阿里云控制台
2. ECS → 网络与安全 → 安全组
3. 配置规则 → 添加安全组规则
4. 开放端口：80、3001

---

## 第八步：访问项目

在浏览器访问：
```
http://你的服务器IP
```

应该能看到登录页面了！

---

## 常用管理命令

### 查看后端日志
```bash
pm2 logs student-api
```

### 重启后端
```bash
pm2 restart student-api
```

### 停止后端
```bash
pm2 stop student-api
```

### 查看进程状态
```bash
pm2 status
```

### 重启Nginx
```bash
systemctl restart nginx
```

### 查看Nginx日志
```bash
tail -f /var/log/nginx/error.log
```

---

## 可选：配置HTTPS（使用Let's Encrypt免费证书）

### 1. 安装Certbot
```bash
# CentOS
yum install certbot python3-certbot-nginx -y

# Ubuntu
apt install certbot python3-certbot-nginx -y
```

### 2. 获取证书
```bash
certbot --nginx -d 你的域名
```

### 3. 自动续期
```bash
certbot renew --dry-run
```

---

## 故障排查

### 1. 后端无法启动
```bash
# 查看日志
pm2 logs student-api

# 检查端口占用
netstat -tunlp | grep 3001

# 检查MySQL连接
mysql -u root -p
```

### 2. 前端无法访问
```bash
# 检查Nginx状态
systemctl status nginx

# 检查Nginx配置
nginx -t

# 查看错误日志
tail -f /var/log/nginx/error.log
```

### 3. 数据库连接失败
- 检查 `server/.env` 配置是否正确
- 检查MySQL是否启动：`systemctl status mysqld`
- 检查数据库是否存在：`mysql -u root -p -e "SHOW DATABASES;"`

---

## 性能优化建议

1. **启用Gzip压缩**（在Nginx配置中添加）
2. **配置静态资源缓存**
3. **使用CDN加速**
4. **数据库索引优化**
5. **定期备份数据库**

---

## 备份脚本

创建自动备份脚本：
```bash
vi /root/backup.sh
```

内容：
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -u root -p你的密码 student_management > /root/backups/db_$DATE.sql
# 保留最近7天的备份
find /root/backups -name "db_*.sql" -mtime +7 -delete
```

设置定时任务：
```bash
chmod +x /root/backup.sh
crontab -e
# 添加：每天凌晨2点备份
0 2 * * * /root/backup.sh
```

---

## 第九步：域名配置（可选）

如果你有域名，可以配置域名访问：

### 1. 域名解析
在域名服务商（如阿里云）添加A记录：
- 记录类型：A
- 主机记录：@ 或 www
- 记录值：你的服务器IP
- TTL：10分钟

### 2. 修改Nginx配置
```bash
vi /etc/nginx/conf.d/student-management.conf
```

修改 `server_name`：
```nginx
server_name www.yourdomain.com yourdomain.com;
```

### 3. 重启Nginx
```bash
nginx -t
systemctl restart nginx
```

---

## 第十步：监控与日志

### 1. 设置日志轮转
```bash
vi /etc/logrotate.d/student-api
```

添加：
```
/var/www/student-management-system/server/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    create 0640 root root
    sharedscripts
}
```

### 2. 监控服务器资源
```bash
# 安装htop
yum install htop -y  # CentOS
apt install htop -y  # Ubuntu

# 查看资源使用
htop
```

### 3. 设置告警（可选）
使用阿里云监控服务：
- 登录阿里云控制台
- 云监控 → 主机监控
- 设置CPU、内存、磁盘告警规则

---

## 第十一步：安全加固

### 1. 修改SSH端口
```bash
vi /etc/ssh/sshd_config
# 修改 Port 22 为其他端口，如 2222
systemctl restart sshd
```

### 2. 禁用root远程登录（推荐）
```bash
# 创建新用户
adduser admin
passwd admin
usermod -aG wheel admin  # CentOS
usermod -aG sudo admin   # Ubuntu

# 修改SSH配置
vi /etc/ssh/sshd_config
# 设置 PermitRootLogin no
systemctl restart sshd
```

### 3. 安装fail2ban防暴力破解
```bash
# CentOS
yum install epel-release -y
yum install fail2ban -y

# Ubuntu
apt install fail2ban -y

# 启动服务
systemctl start fail2ban
systemctl enable fail2ban
```

### 4. 配置MySQL安全
```bash
mysql_secure_installation
```

按提示完成：
- 设置密码强度
- 删除匿名用户
- 禁止root远程登录
- 删除测试数据库

---

## 更新部署流程

当代码更新后，使用以下流程重新部署：

### 快速更新脚本
创建更新脚本：
```bash
vi /var/www/student-management-system/deploy.sh
```

内容：
```bash
#!/bin/bash
echo "开始部署..."

# 1. 拉取最新代码
cd /var/www/student-management-system
git pull origin main

# 2. 更新后端
cd server
npm install
pm2 restart student-api

# 3. 更新前端
cd ..
npm install
npm run build

# 4. 重启Nginx
systemctl restart nginx

echo "部署完成！"
```

设置权限并执行：
```bash
chmod +x /var/www/student-management-system/deploy.sh
/var/www/student-management-system/deploy.sh
```

---

## 数据库管理

### 1. 安装phpMyAdmin（可选）
```bash
# CentOS
yum install phpmyadmin -y

# Ubuntu
apt install phpmyadmin -y
```

### 2. 使用命令行管理
```bash
# 导出数据库
mysqldump -u root -p student_management > backup.sql

# 导入数据库
mysql -u root -p student_management < backup.sql

# 查看表结构
mysql -u root -p -e "USE student_management; SHOW TABLES;"

# 查看数据
mysql -u root -p -e "USE student_management; SELECT * FROM users;"
```

### 3. 定期优化数据库
```bash
# 创建优化脚本
vi /root/optimize-db.sh
```

内容：
```bash
#!/bin/bash
mysql -u root -p你的密码 -e "
USE student_management;
OPTIMIZE TABLE users;
OPTIMIZE TABLE students;
OPTIMIZE TABLE courses;
OPTIMIZE TABLE scores;
OPTIMIZE TABLE attendance;
OPTIMIZE TABLE notifications;
"
echo "数据库优化完成"
```

设置每周执行：
```bash
chmod +x /root/optimize-db.sh
crontab -e
# 添加：每周日凌晨3点优化
0 3 * * 0 /root/optimize-db.sh
```

---

## 性能测试

### 1. 使用ab进行压力测试
```bash
# 安装Apache Bench
yum install httpd-tools -y  # CentOS
apt install apache2-utils -y  # Ubuntu

# 测试首页
ab -n 1000 -c 10 http://你的服务器IP/

# 测试API
ab -n 1000 -c 10 http://你的服务器IP/api/students
```

### 2. 查看响应时间
```bash
curl -w "@-" -o /dev/null -s http://你的服务器IP/ <<'EOF'
    time_namelookup:  %{time_namelookup}\n
       time_connect:  %{time_connect}\n
    time_appconnect:  %{time_appconnect}\n
      time_redirect:  %{time_redirect}\n
   time_pretransfer:  %{time_pretransfer}\n
 time_starttransfer:  %{time_starttransfer}\n
                    ----------\n
         time_total:  %{time_total}\n
EOF
```

---

## 常见问题解答

### Q1: 忘记MySQL密码怎么办？
```bash
# 停止MySQL
systemctl stop mysqld

# 跳过权限验证启动
mysqld_safe --skip-grant-tables &

# 登录并修改密码
mysql -u root
USE mysql;
UPDATE user SET authentication_string=PASSWORD('新密码') WHERE User='root';
FLUSH PRIVILEGES;
exit;

# 重启MySQL
systemctl restart mysqld
```

### Q2: 端口被占用怎么办？
```bash
# 查看占用端口的进程
netstat -tunlp | grep 3001

# 杀死进程
kill -9 进程ID

# 或者修改端口
vi /var/www/student-management-system/server/.env
# 修改 PORT=3002
```

### Q3: 磁盘空间不足？
```bash
# 查看磁盘使用
df -h

# 查找大文件
du -sh /* | sort -rh | head -10

# 清理日志
journalctl --vacuum-time=7d
rm -rf /var/log/*.log.gz

# 清理npm缓存
npm cache clean --force
```

### Q4: 内存不足？
```bash
# 创建swap交换空间
dd if=/dev/zero of=/swapfile bs=1M count=2048
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# 永久生效
echo '/swapfile none swap sw 0 0' >> /etc/fstab
```

### Q5: 如何查看访问日志？
```bash
# Nginx访问日志
tail -f /var/log/nginx/access.log

# 后端日志
pm2 logs student-api

# 实时监控
pm2 monit
```

---

## 完成！

现在你的学生管理系统已经成功部署到阿里云了！🎉

### 访问地址
- HTTP访问：`http://你的服务器IP`
- 域名访问：`http://你的域名`（如已配置）
- HTTPS访问：`https://你的域名`（如已配置SSL）

### 默认登录信息
- 用户名：`admin`
- 密码：`admin123`

**重要提醒：** 首次登录后请立即修改默认密码！

### 技术支持
如遇到问题，请检查：
1. 后端日志：`pm2 logs student-api`
2. Nginx日志：`tail -f /var/log/nginx/error.log`
3. MySQL状态：`systemctl status mysqld`
4. 防火墙规则：阿里云安全组配置

---

## 附录：完整部署检查清单

- [ ] 服务器环境安装完成（Node.js、MySQL、Nginx）
- [ ] 项目文件上传完成
- [ ] 数据库创建并导入数据
- [ ] 后端配置文件修改（.env）
- [ ] 后端服务启动（PM2）
- [ ] 前端构建完成（npm run build）
- [ ] Nginx配置完成
- [ ] 防火墙端口开放
- [ ] 阿里云安全组配置
- [ ] 可以正常访问登录页面
- [ ] 可以正常登录系统
- [ ] 各功能模块测试通过
- [ ] 配置自动备份
- [ ] 配置HTTPS（可选）
- [ ] 配置域名（可选）
- [ ] 修改默认密码

全部完成后，你的系统就可以正式上线使用了！
