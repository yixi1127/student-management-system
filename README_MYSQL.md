# 学生管理系统 - MySQL 版本

基于 Vue3 + Vite + Element Plus + MySQL + Express 开发的现代化学生管理系统。

## ✨ 技术栈

### 前端
- Vue 3.5 - 渐进式 JavaScript 框架
- Vite 7.2 - 下一代前端构建工具
- Element Plus - Vue 3 组件库
- Pinia - Vue 状态管理
- Vue Router - 路由管理
- Axios - HTTP 客户端
- ECharts - 数据可视化

### 后端
- Node.js - JavaScript 运行环境
- Express - Web 应用框架
- MySQL - 关系型数据库
- JWT - 身份认证
- bcryptjs - 密码加密

## 🚀 快速开始

### 前置要求

- Node.js 18+
- MySQL 8.0+
- MySQL Workbench（推荐）

### 1. 克隆项目

```bash
git clone <your-repo>
cd student-management-system
```

### 2. 安装前端依赖

```bash
npm install
```

### 3. 配置数据库

#### 使用 MySQL Workbench

1. 打开 MySQL Workbench
2. 连接到本地 MySQL
3. 打开 `database-mysql.sql`
4. 执行脚本创建数据库和表

#### 或使用命令行

```bash
mysql -u root -p < database-mysql.sql
```

### 4. 配置后端

```bash
cd server
npm install
```

编辑 `server/.env`：

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的密码
DB_NAME=student_management
JWT_SECRET=your-secret-key
```

### 5. 启动服务

#### 启动后端（终端1）

```bash
cd server
npm run dev
```

#### 启动前端（终端2）

```bash
npm run dev
```

### 6. 访问系统

打开浏览器访问：http://localhost:3000

测试账号：
- 管理员：admin@example.com / 123456
- 教师：teacher1@example.com / 123456
- 学生：zhangsan@example.com / 123456

## 📁 项目结构

```
student-management-system/
├── src/                      # 前端源码
│   ├── views/               # 页面组件
│   │   ├── Login.vue        # 登录页
│   │   ├── Layout.vue       # 主布局
│   │   ├── Dashboard.vue    # 数据看板
│   │   ├── Students.vue     # 学生管理
│   │   ├── Courses.vue      # 课程管理
│   │   ├── Scores.vue       # 成绩管理
│   │   ├── Attendance.vue   # 考勤管理
│   │   ├── Notifications.vue # 通知管理
│   │   └── Profile.vue      # 个人中心
│   ├── router/              # 路由配置
│   ├── stores/              # 状态管理
│   ├── utils/               # 工具函数
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── server/                   # 后端源码
│   ├── routes/              # API 路由
│   │   ├── auth.js          # 认证
│   │   ├── students.js      # 学生
│   │   ├── courses.js       # 课程
│   │   ├── scores.js        # 成绩
│   │   ├── attendance.js    # 考勤
│   │   ├── notifications.js # 通知
│   │   └── dashboard.js     # 统计
│   ├── config/              # 配置
│   │   └── db.js            # 数据库连接
│   ├── middleware/          # 中间件
│   │   └── auth.js          # 认证中间件
│   ├── index.js             # 入口文件
│   ├── package.json         # 依赖配置
│   └── .env                 # 环境变量
├── database-mysql.sql       # MySQL 数据库脚本
├── MYSQL_SETUP.md          # MySQL 配置指南
└── package.json            # 前端依赖配置
```

## 🎯 功能特性

### 已实现功能

✅ 用户认证（登录/注册）  
✅ 数据看板（统计图表）  
✅ 学生管理（增删改查、搜索、分页）  
✅ 课程管理（列表查看）  
✅ 成绩管理（列表查看）  
✅ 考勤管理（列表查看）  
✅ 通知管理（列表查看）  
✅ 个人中心（信息修改）  
✅ 权限控制（基于角色）  

### 待完善功能

⏳ 课程的增删改  
⏳ 成绩的录入和修改  
⏳ 考勤的录入和修改  
⏳ 通知的发布和管理  
⏳ 文件上传功能  
⏳ 数据导入导出  
⏳ 密码加密（bcrypt）  

## 📊 数据库设计

### 核心表

- **users** - 用户表（管理员、教师、学生）
- **students** - 学生详细信息
- **courses** - 课程信息
- **student_courses** - 选课关系
- **scores** - 成绩记录
- **attendance** - 考勤记录
- **notifications** - 通知公告
- **notification_reads** - 通知已读记录

### 测试数据

数据库脚本已包含测试数据：
- 1个管理员
- 2个教师
- 4个学生
- 4门课程
- 若干成绩和考勤记录

## 🔌 API 接口

### 认证接口

```
POST /api/auth/login      # 登录
POST /api/auth/register   # 注册
```

### 学生接口

```
GET    /api/students           # 获取列表（支持搜索、分页）
POST   /api/students           # 添加学生
PUT    /api/students/:id       # 更新学生
DELETE /api/students/:id       # 删除学生
```

### 其他接口

```
GET /api/courses              # 课程列表
GET /api/scores               # 成绩列表
GET /api/attendance           # 考勤列表
GET /api/notifications        # 通知列表
GET /api/dashboard/stats      # 统计数据
```

## 🎨 界面预览

- 渐变色主题设计
- 响应式布局
- 卡片式组件
- ECharts 数据可视化
- 流畅的动画效果

## 🔒 安全特性

- JWT Token 认证
- 路由权限守卫
- 基于角色的访问控制
- SQL 注入防护（参数化查询）
- CORS 跨域配置

## 📝 开发说明

### 添加新的 API 接口

1. 在 `server/routes/` 创建路由文件
2. 在 `server/index.js` 注册路由
3. 在前端 `utils/request.js` 调用接口

### 添加新页面

1. 在 `src/views/` 创建 Vue 组件
2. 在 `src/router/index.js` 添加路由
3. 在 `src/views/Layout.vue` 添加菜单项

### 数据库迁移

修改数据库结构后：
1. 更新 `database-mysql.sql`
2. 在 MySQL Workbench 执行新的 SQL
3. 更新相关的 API 接口

## 🐛 常见问题

详见 [MYSQL_SETUP.md](./MYSQL_SETUP.md)

## 📚 相关文档

- [MySQL 配置指南](./MYSQL_SETUP.md) - 详细的安装和配置步骤
- [需求文档](../need/need.md) - 完整的需求说明
- [数据库脚本](./database-mysql.sql) - MySQL 建表脚本

## 🚢 部署

### 前端部署

```bash
npm run build
# 将 dist 目录部署到静态服务器
```

### 后端部署

```bash
cd server
npm start
# 使用 PM2 或其他进程管理工具
```

### 数据库部署

- 使用阿里云 RDS MySQL
- 或自建 MySQL 服务器
- 执行 `database-mysql.sql` 初始化

## 📄 许可证

MIT License

## 👥 贡献

欢迎提交 Issue 和 Pull Request

---

**开发日期**: 2024-01-06  
**版本**: v1.0.0  
**技术栈**: Vue3 + MySQL + Express
